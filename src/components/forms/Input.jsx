import * as React from "react";
import styled from "styled-components";

function Input({
  name,
  type,
  label,
  optional,
  placeholder,
  parseInput,
  className,
  children,
  ...props
}) {
  // draws error
  // draws
}

function InputError({ name }) {}

function InputLabel() {}

export { Input };
